---
title: "R Notebook"
output: html_notebook
---

```{r}
library(vegan)
```


```{r}
vareveg <- read.csv('../data/vareveg.csv')
vareenv <- read.csv('../data/vareenv.csv')
head(vareenv)
```

```{r}
vareveg.dist <- vegdist(wisconsin(vareveg[,-1]^0.25), 'bray')
vareenv.std <- decostand(vareenv[,-1], "standardize") 
```

```{r}
bioenv(vareveg.dist, vareenv.std)
```

```{r}
adonis(vareveg.dist~Ca+Fe+Mn+Baresoil, data = vareenv.std)
#besides baresoil, the other variables are all useful drivers of the community
```

